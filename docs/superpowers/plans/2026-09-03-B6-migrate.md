# B6. Перехід двох репозиторіїв 0.6.0 на маніфест — план реалізації

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** довести `SMP_SimplyConnect` і `SMP_BankExchange` до маніфесту `v8storagekit.yaml`
штатними засобами — скілом `onboarding` і командою `sync`, — а після переходу обох прибрати
з kit усе, що існувало заради перехідної форми.

**Architecture:** **команди `migrate` немає і не буде** (рішення користувача, спека `2f2da62`, §9).
Інвентаризація парку показала, що вона покривала б 2 репозиторії з 10, і обидва — тимчасові.
Усе, що вона робила б, роблять штатні шляхи: маніфест і накладку пише `onboarding`; гілки
`storage/<ім'я>` — **перший** `sync` повторним реплеєм на порожню orphan-гілку (це не міграційний
шлях, а звичайний); перше злиття — `--allow-unrelated-histories`, теж штатне. Специфічного для
переходу лишається рівно два кроки, і обидва — не інструмент, а поведінка скіла `onboarding`
(пишеться в B5): **підказки зі спадку** (прочитати `storage.json` і `devInfobase:`, запропонувати
як відповіді на власні питання, людина підтверджує — §2.5 «питаємо, не виводимо») і **видалення
спадку** після запису маніфесту. Історія старого реплею на головній гілці лишається як архів (§9).

**Tech Stack:** скіли kit, `sync`, `verify`, `install-hooks`; платформа 1С (реплей сховищ).

**Spec:** `docs/superpowers/specs/2026-09-03-agent-contour-design.md` — §9 (перехід без окремої
команди), §6 (`onboarding` питає поіменно), §2.2 (ім'я розширення в трьох місцях), §3.4 (перше
злиття), §13 (EDT поза контуром; OnlineExchange — окремо), §14 (B6).

> **⚠ §9 ПЕРЕПИСУЄТЬСЯ — цей план не починати до нової редакції** (2026-09-09).
>
> Рішення `2f2da62` про EDT виявилось помилковим, користувач поправив архітектора напряму:
> **вісім «EDT-репозиторіїв» — це gitsync-вивантаження СХОВИЩ** (квартет
> `AUTHORS`+`VERSION`+`DT-INF/`+`ConfigDumpInfo.xml`, зроблені колись «для історії»), а не
> EDT-розробка. Істина для них — сховище, і саме вони **головний вхід міграції**. Конверсія
> EDT → Designer не потрібна: старий скіл `repo-migration` робив саме це (orphan-гілка, реплей зі
> сховища, старе дерево не чіпається) і пройшов на `BankExchange` та `SimplyConnect`.
>
> З цього плану чинним лишається **тільки «не окрема команда»**. Обсяг блоку в новій редакції —
> три вхідні форми, усі в діалозі з людиною: порожній репозиторій зі сховищами (новий проєкт),
> gitsync/EDT-репозиторій (**вісім** — головний випадок), напівмігрований 0.6.0 (два, тимчасово).
> Довідку зі старого `repo-migration` **не видаляти** — вона стане основою.

## Global Constraints

- **Порядок доступу до живих репозиторіїв:** спершу `<TEST_REPO>` (Task 1), і лише після його
  успіху — `SMP_SimplyConnect`, потім `SMP_BankExchange`, кожен за **окремим** підтвердженням
  користувача. Двох живих репозиторіїв одразу не брати.
- **Страхувальний `git bundle create` перед будь-якою зміною** будь-якого репозиторію, включно з
  живими; шлях bundle — у доповідь. Без нього не починати: команди `migrate` з її власними
  запобіжниками більше немає, і bundle лишився єдиною страховкою.
- Сховища — тільки читання; `-Apply` — лише на прохання; `git push` — ні; версію не піднімати.
- Мова — українська; ASCII-якорі; правило `${CLAUDE_PLUGIN_ROOT}`; префікси скілів.
- Локальний маркетплейс уже стоїть (B1 Task 10): скіли `onboarding`, `sync`, `verify` доступні в
  сесії будь-якого з цих репозиторіїв.
- **Kit не покладається на git-конфіг машини споживача (принцип 7 спеки; знахідка живого прогону B2):**
  `core.quotepath` типово `true`, і git екранує неASCII-шляхи вісімково в лапках у `ls-tree`, `ls-files`,
  `status`, `diff` — на репозиторії 1С це 240+ хибних «файлів поза шляхом». Правило (рев'ю B2):
  `-c core.quotepath=false` — на **кожному** git-виклику, що читає або друкує шляхи, без винятків;
  **`-z` додатково** — там, де викликач ділить вивід на записи **і** запис міг створити не kit (довільний ref,
  довільна робоча копія): `Export-KitTree`, `Get-KitBinaryPaths`, `canon status`. Пояснення: `quotepath`
  керує лише байтами ≥ 0x80, а `"`, `\` і перевід рядка git C-квотує завжди — `-z` прибирає саме це.
  Фікстурам заборонено виставляти `core.quotepath` — це маскувало б дефект (у SMP_BankExchange він був
  замаскований локальним `.git/config`).
- **`Mock -ModuleName` не перетинає межу модуля (знахідка B3):** щойно виклик платформи переїжджає з
  `commands/X.psm1` у `lib/Y.psm1`, моки `-ModuleName X` мовчки перестають перехоплювати — тести лишаються
  зеленими й тихо піднімають справжній `1cv8.exe`. Приймальна ознака тесту без платформи — не час виконання, а
  явне `Should -Invoke … -ModuleName <модуль, де ТЕПЕР живе виклик>`; при кожному перенесенні коду між модулями
  моки переадресовуються.
- **`@(Get-ChildItem …).Count`, без винятків (знахідка B3, тричі):** під `Set-StrictMode -Version Latest`
  `(Get-ChildItem …).Count` кидає `PropertyNotFoundException` не лише на порожній теці, а й на теці **рівно з
  одним файлом** — `Get-ChildItem` повертає скалярний `FileInfo` без `.Count` (перевірено на pwsh 7.5.4).
  У продакшні це мовчить лише тому, що дамп конфігурації дає тисячі файлів. Те саме для будь-якого
  конвеєра, що може дати один елемент.
- **Уроки B1, обов'язкові для виконавця:**
  1. *Приймальна ознака попереджень* — не рахунок рядків `warning:` (недетермінований, змішує навмисне з
     випадковим), а іменна: «жоден `warning:` не називає файлу з цього diff'у».
  2. *Дисципліна коміту в спільній робочій копії* — лише `git add <явний перелік>` +
     `git commit --only -- <ті самі шляхи>`; ніяких `-a`/`-A`; після `git update-index` — комітити негайно.
  3. *Кеш плагіна — знімок, не лінк:* після кожного блоку (і перед живою перевіркою скілів/хука) —
     `claude plugin install v8storagekit@smp-v8storagekit` (або `update`) і **нова сесія**; без цього сесії
     бачать попередню версію робочої копії.

### Рішення, узгоджені з архітектором

| # | Питання | Рішення | Де в спеці |
|---|---|---|---|
| Q7 | де прогін | тестовий репозиторій від користувача; план параметризований `<TEST_REPO>`; живі — після зворотного зв'язку, окремим рішенням | §14 |
| — | дзеркала | повторним реплеєм зі сховища (`sync`), не `filter-repo`; історія реплею 0.6.0 на головній гілці — архів | §9 |
| — | конфлікт першого злиття | дерева мають збігатись; якщо ні (стара політика тексту) — зупинка, звірити `verify -Ref storage/X`, розв'язувати на боці сховища | §9, §3.4 |

---

- **Пастки, доведені в B1–B3 (кожна була Critical у рев'ю, і кожна прийшла з тексту плану):**
  - `WaitForExit()` **завжди з таймаутом** — без нього дефект дає зависання назавжди, а не помилку.
  - **Кодування задавати явно** там, де читається вивід git з іменами файлів: амбієнтне
    `[Console]::OutputEncoding` вірне лише всередині `kit.ps1`, а модулі викликають і тести, і
    хук — кирилиця псується мовчки.
  - `[Parameter(Mandatory)]` на **колекції** — разом з `[AllowEmptyCollection()]`: порожній масив
    тут типовий вхід (розширення без binary-файлів), а не крайній випадок.
  - `Measure-Object` і `.Count` на порожньому чи одноелементному вводі — через `@()`.
  - **Порівняння часу — з допуском:** файлова система і платформа дають різну гранулярність
    `mtime`, без допуску сигнал стає вічно-хибним.
  - Зовнішній виклик, що **змінює стан** (прив'язка до сховища, злиття, запуск платформи), — у
    `try`/`finally` зі звільненням у `finally`; інакше перший виняток лишає стан підвішеним.
  - **Тест мусить падати, якщо вирізати production-код.** Тест, що дублює формулу всередині себе
    або перевіряє поведінку зовнішнього інструмента, проходить і з видаленою командою — це вже
    ловили (`docs/follow-ups.md` §4).

## Як виконувати цей план (методика, ухвалена після B3)

B3 коштував ~3,3 млн токенів у субагентах і ~35 запусків агентів на ~2000 змінених рядків.
Розбір показав, що дорога не дрібність задач, а **рівномірна строгість**: однакове подвійне
рев'ю діставалось і задачі на 44 рядки, і задачі, що чіпає спільний диспетчер. Звідси правила.

- **Рев'ю за рівнем ризику, а не за фактом «задача завершена».** Кожна задача починається
  рядком `**Ризик:**` з одним із трьох рівнів:
  - `механічна` — вилучення файлів, тексти скілів, документація, перейменування. **Одне рев'ю
    на дешевій моделі**, і воно перевіряє не логіку, а guard-умови: `grep`-перевірки кроку
    «Прогін», префікс `v8storagekit:` у перехресних посиланнях, `${CLAUDE_PLUGIN_ROOT}` лише
    всередині шляху, наявність рядків меж у тексті скіла.
  - `спільний код` — чіпає диспетчер, модуль `lib/*.psm1`, який викликають інші команди, або
    формат, що читають сусідні задачі. **Одне рев'ю на сильній моделі.**
  - `властивість безпеки` — задача тримає межу зі спеки (агент не пише в базу людини; сховище
    лише читання; `-Apply` тільки на явне прохання; переписування чужого репозиторію).
    **Два рев'ю**, друге — вузьке, лише про цю властивість.
- **Код у плані — ескіз, а не транскрипція.** Усі чотири Critical, знайдені в B3, прийшли з
  тексту плану, а не з помилок виконавців: виконавці брали код дослівно, як і вимагалось.
  Детальність не знизила частку дефектів — вона перенесла їх із виконання в рев'ю, де довести
  дефект дорожче (прочитати код, відтворити сценарій, показати наслідок). Тому **рев'ю ставиться
  до коду з плану так само, як до свіжого коду виконавця**. Судження «це з плану, отже вже
  перевірене» хибне: такого статусу код у плані не має.
- **Текст задачі заморожений на час її виконання.** Автор планів, поки задача в роботі, вносить
  у файл **лише розблокувальні** правки — прямі відповіді на `NEEDS_CONTEXT` від виконавця. Усе
  інше (уточнення, покращення, наслідки свіжих рішень спеки) чекає кінця задачі й іде однією
  редакцією. У B3 коди виходу `session-check` переглядались **п'ять разів усередині роботи**, і
  один запуск виконавця згорів на 131 тис. токенів через суперечність, внесену проміжною
  редакцією. Це стосується і правок, що приходять зі спеки: нове рішення архітектора лягає в
  хвіст блоку, а не в живу задачу.
- **`Integration` — один прогін на блок**, у фінальній задачі, за підтвердженням користувача.
  Самі `Integration`-тести пишуться у своїх задачах і позначаються тегом, але **не запускаються**
  доти: живий прогін платформи дорогий і дає ту саму інформацію, зібраний в одну точку.
- **Дрібна механічна задача окремо не існує** — вона в складі задачі-споживача або фінальної
  задачі блоку. Окремо вона коштує повний цикл (виконавець + рев'ю + звіт) за 40 рядків змін.

## File Structure

| Файл | Відповідальність | Задача |
|---|---|---|
| `<TEST_REPO>` | прогін `onboarding` на **чистому** репозиторії — головний випадок скіла | 1 |
| `SMP_SimplyConnect`, `SMP_BankExchange` | перехід зі спадку, по одному, кожен за окремим підтвердженням | 2 |
| `skills/onboarding/SKILL.md` | розділ «спадок 0.6.0» **вилучається** після переходу обох | 3 |
| `docs/migration/2026-09-02-legacy-repo-rename-prompt.md` | **вилучається** (§9) | 3 |
| `docs/follow-ups.md` | §10 — результат щодо кириличних імен | 3 |

Коду в цьому блоці немає: `onboarding` пишеться в B5, `sync`/`verify`/`install-hooks` існують
з B2–B5. Якщо в задачі виникає потреба щось **дописати** в kit — це сигнал, що B5 неповний, і
правити треба там, а не тут.

---

### Task 1: `onboarding` на чистому тестовому репозиторії (за підтвердженням користувача)

**Ризик:** `механічна` — прогін, коду немає. Рев'ю не потрібне; потрібна доповідь користувачу з
дослівними виводами, а не переказом.

`<TEST_REPO>` — шлях, який дає користувач. **Чистий** репозиторій (без `storage.json` і
`v8project.yaml`) тут головний випадок, а не крайній: саме на ньому перевіряється, що скіл питає
поіменно, бо підказок зі спадку взяти нізвідки.

- [ ] **Step 1:** `git bundle create` перед будь-якою зміною; записати шлях.
- [ ] **Step 2:** Сесія в корені `<TEST_REPO>`, скіл `v8storagekit:onboarding`. Пройти розділи 1–3:
      маніфест, воркспейс, джерела з `truth` кожного, політики git, хуки, перший коміт.
- [ ] **Step 3:** `kit check` — без `[-]`. Кожна знахідка, що лишилась, — у доповідь.
- [ ] **Step 4:** `sync -Source <ключ> -Apply` для одного джерела; далі `-MergeMain` (перше злиття).
      Зафіксувати час і кількість версій.
- [ ] **Step 5:** `verify` — очікувано `equal`. Інакше **не «лагодити» на місці**: зібрати вивід
      класифікації й доповісти. Це і є зворотний зв'язок, заради якого тестовий репозиторій існує.
- [ ] **Step 6:** Нова сесія в `<TEST_REPO>`: хук показує «Стан сховищ» без сигналів.
- [ ] **Step 7:** Доповідь користувачу: що скіл спитав, чого не спитав і мусив би, які кроки
      людина робила руками попри те, що їх мав вести скіл. Останнє важливіше за перші два: воно
      каже, чого бракує в B5.

---

### Task 2: перехід `SMP_SimplyConnect` і `SMP_BankExchange` (за окремим підтвердженням на кожен)

**Ризик:** `властивість безпеки` — це **живі репозиторії людини**. Помилка коштує не прогону, а
чужих даних. Кожен репозиторій — окреме підтвердження користувача; двох одразу не брати.

Порядок: спершу `SMP_SimplyConnect` (2 `storage.json`, 2 `v8project.yaml` — менший), потім
`SMP_BankExchange` (3 і 4). Другий робиться лише після того, як перший визнано успішним.

- [ ] **Step 1:** `git bundle create` **обов'язково**; шлях — у доповідь. Без bundle не починати.
- [ ] **Step 2:** `onboarding` із підказками зі спадку: скіл читає `storage.json` (шлях сховища,
      користувач, ім'я розширення) і `devInfobase:` з `v8project.local.yaml`, пропонує їх як
      відповіді, людина **підтверджує кожну**. Мовчазного перенесення немає.
- [ ] **Step 3:** Спадок видаляється після запису маніфесту й накладки: `storage.json`, стан
      `SyncState`, `devInfobase:` з `v8project.local.yaml` (сам файл лишається, якщо в ньому є
      серверна база агента). Перевірити, що `check` більше не дає `warn` про `devInfobase:`
      (знахідка B4 Task 5 Step 2б).
- [ ] **Step 4:** Ім'я розширення в трьох місцях (§2.2): тека, `name:` source-set, `Configuration.xml`.
      Де тека ≠ ім'я розширення — перейменувати; `check` це показує.
- [ ] **Step 5:** `sync -Apply` для кожного джерела, за потреби `-MaxVersions`; перше злиття.
- [ ] **Step 6:** `verify` кожного джерела — `equal`. Розбіжності не правити на місці: доповісти.
- [ ] **Step 7:** Спадщина кореневого `build/`, зафіксована прогоном B4: `build/build-ib` (стара
      база агента **в корені**, не у воркспейсі), `build/sync` із п'ятьма теками — три під
      **старими** іменами продуктів, `build/artifacts` із `.cfe` від старого `build.ps1`. Тека
      гітігнорована, тож рішення дешеве: **запитати користувача** — лишити чи почистити. Не
      вирішувати самостійно: там можуть бути артефакти, які комусь ще потрібні.
- [ ] **Step 8:** Доповідь по кожному репозиторію окремо.

---

### Task 3: прибирання після переходу обох

**Ризик:** `механічна` — вилучення й документація.

Виконується **лише** після того, як обидва репозиторії перейшли й визнані робочими. Доти
підказки зі спадку потрібні.

- [ ] **Step 1:** `skills/onboarding/SKILL.md` — вилучити розділ «спадок 0.6.0» (підказки зі
      `storage.json` і `devInfobase:`). Перехідна форма закінчилась разом із цими двома
      репозиторіями; тримати в скілі шлях, якого більше не буває, — платити увагою читача.
      Guard-тест на цей розділ (B5) вилучити разом.
- [ ] **Step 2:** `git rm docs/migration/2026-09-02-legacy-repo-rename-prompt.md` — разовий промпт
      0.6.0 (§9). `docs/migration/legacy-gitsync-repo.md` **лишається** до рішення по
      `SMP_OnlineExchange`.
- [ ] **Step 3:** `docs/follow-ups.md` §10 — дописати результат щодо кириличних імен, якщо він був.
- [ ] **Step 4:** Тести без `Integration` зелені; коміт.

---

## Свідомо не в цьому блоці

**⚠ РОЗДІЛ СКАСОВАНО** (2026-09-09): ці вісім — gitsync-вивантаження сховищ і **головний вхід
міграції**, а не «поза контуром». Текст лишено як запис хибного рішення, щоб було видно, що саме
переглядається, і чекає нової редакції §9.

~~**Вісім EDT-репозиторіїв** (`SMP_OnlineExchange`, `SMP_SimplyDelivery`, `SMP_SimplyAutomate`,
`SMP_SimplyCatalog`, `SMP_SimplyMarketplace`, `SMP_SMB_ukr_DEV`, `SMP_SMB_rus_DEV`,
`SMB_ukr_vendor`) — поза контуром, і це межа, а не діра в маршруті (§13).~~ Контур свідомо
побудований для Designer platform XML; kit не конвертує, `convert` Уніки applied fail-closed.
Переведення парку на Designer XML — рішення користувача про формат вихідників, і якщо воно буде
ухвалене, це окрема робота з власною спекою, не B6.

## Self-review

| Вимога спеки (§9, §14) | Задача |
|---|---|
| перехід без окремої команди: маніфест і накладка — `onboarding`, гілки — `sync`, злиття — штатне | 1, 2 |
| підказки зі спадку (`storage.json`, `devInfobase:`) — пропозиція, яку людина підтверджує | 2 |
| спадок видаляється після запису маніфесту | 2 |
| ім'я розширення в трьох місцях (§2.2) | 2 |
| `verify` кожного джерела — нуль змістовних розбіжностей | 1, 2 |
| тестовий репозиторій, не живі, для першого прогону; параметризація `<TEST_REPO>` | 1 |
| разовий промпт 0.6.0 вилучається (§9) | 3 |
| підказки зі спадку вилучаються після переходу обох | 3 |
| EDT поза контуром (§13); `SMP_OnlineExchange` — окреме рішення | — |

**Чого тут немає навмисно:** `tools/commands/migrate.psm1`, `Migrate.Tests.ps1`,
`New-KitLegacyRepo`, `skills/migrate/`. Якщо в задачі з'явилась потреба їх створити — це означає,
що штатний шлях чогось не покриває, і правильна реакція — сказати про це, а не написати команду.
